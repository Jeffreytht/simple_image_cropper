enum ImageEditorMode {
  /// When the corner grabber haven't been drawn
  none,

  /// When the corner grabber is drawn on screen
  boudingRectReady,

  /// When user is resizing the corner grabber
  resizing,

  /// When user is moving the corner grabber
  moving
}
